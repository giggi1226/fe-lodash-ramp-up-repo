import isEmpty from 'lodash/fp/isEmpty';

describe('lodash/fp/isEmpty', () => {
  it('should check if value is an empty object, collection, map, or set', () => {
    const nullValue = null;
    const isOne = 1;
    const array = [1, 2, 3];
    const object = { a: 1 };
  });
});
