import merge from 'lodash/fp/merge';

describe('lodash/fp/merge', () => {
  it('should merge objects together', () => {
    const objectOne = {
      name: 'name',
      email: 'email@email.com'
    };
    const objectTwo = {
      address: '13 N Street'
    };
  });
});
