import mergeAll from 'lodash/fp/mergeAll';

describe('lodash/fp/mergeAll', () => {
  it('should merge all objects together', () => {
    const objectOne = { a: 1 };
    const objectTwo = { b: 2 };
    const objectThree = { c: 3 };
    const objectFour = { d: 4 };
  });
});
