import get from 'lodash/fp/get';

describe('lodash/fp/get', () => {
  it('should get the specific values out of the object', () => {
    const object = { name: 'Peter', age: 30, height: 185 };
  });
});
