import getOr from 'lodash/fp/getOr';

describe('lodash/fp/getOr', () => {
  it('should return a default value if value does not exist', () => {
    const object = {
      object: {
        name: 'Brandon'
      }
    };
  });
});
